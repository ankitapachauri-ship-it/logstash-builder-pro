version: "3.9"

services:
  logstash-builder:
    build:
      context: .
      dockerfile: Dockerfile
    image: logstash-builder:local
    ports:
      - "127.0.0.1:8080:8080"  # F-10: bind localhost only — no IAP in local Docker
    environment:
      PORT: "8080"
    restart: unless-stopped
