version: "3.9"

services:
  logstash-builder:
    build:
      context: .
      dockerfile: Dockerfile
    image: logstash-builder:local
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
    restart: unless-stopped
